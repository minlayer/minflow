/**
 * Reserved namespace package.
 *
 * The implementation is published as `minflow`. This package exists only so
 * the `@minlayer` scope mirrors the published package names.
 */

/** The current package version. */
export const VERSION = "0.0.0";
