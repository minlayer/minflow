/**
 * Deprecated placeholder.
 *
 * The implementation is published as `minflow`, and this package exists only
 * because the name was reserved before that was settled. It is marked deprecated
 * on the registry, which is where anyone installing it will see it; nothing here
 * is worth reading.
 *
 * Do not publish a new version. Publishing clears the deprecation for the version
 * published, which turns a signpost back into a package that installs and does
 * nothing.
 */

/** The version of this placeholder, which does not track `minflow`. */
export const VERSION = "0.0.0";
